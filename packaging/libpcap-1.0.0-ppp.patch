Index: gencode.c
===================================================================
--- gencode.c.orig
+++ gencode.c
@@ -7483,6 +7483,7 @@ gen_inbound(dir)
 	 */
 	switch (linktype) {
 	case DLT_SLIP:
+	case DLT_PPP:
 		b0 = gen_relation(BPF_JEQ,
 			  gen_load(Q_LINK, gen_loadi(0), 1),
 			  gen_loadi(0),
